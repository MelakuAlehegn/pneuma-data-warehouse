packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]
  - package: metaplane/dbt_expectations
    version: [">=0.10.0", "<1.0.0"]
  # Elementary captures every run + test result into its own schema so we get
  # historical observability (flaky tests, freshness drift, model run times).
  - package: elementary-data/elementary
    version: [">=0.18.0", "<0.19.0"]
